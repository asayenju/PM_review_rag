{
  "name": "pm-review-rag",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "backend:dev": "uvicorn backend.main:app --reload --host 0.0.0.0 --port 4000"
  }
}
